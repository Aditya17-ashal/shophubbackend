# Production configuration for Render deployment
server.port=${PORT:10000}

# MongoDB configuration - use environment variables for security
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://shophub:aditya@cluster0.hegvgfw.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:shophub}

# JWT configuration - use environment variables for security
jwt.secret=${JWT_SECRET:YmtiamVmamFuZHBvJ2Y1MXNmdmJoZmhiYmtmamJzZGprMTRmNjU3NDRqZmpsd25kc24=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Logging configuration
logging.level.com.example.shophubbackend=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN

# Actuator endpoints
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.metrics.export.prometheus.enabled=false

# Security headers
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

# CORS configuration for production
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://your-frontend-domain.com}

# Database connection pool settings
spring.data.mongodb.options.max-connection-pool-size=10
spring.data.mongodb.options.min-connection-pool-size=5
spring.data.mongodb.options.max-connection-idle-time=30000
